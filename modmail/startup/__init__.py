"""Startup and environment bootstrap.

This package contains import-light helpers used by entrypoints to prepare the
runtime environment (event loop policies, optional accelerators) and validate
critical dependencies.
"""
